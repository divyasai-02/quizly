# quizly
Quizly ‑ is a MERN stack quiz application that supports Instructor and Learner roles, class‑scoped private quizzes, public practice quizzes, server‑enforced timers, autosave, and result analytics. It includes starter code for authentication (JWT), quiz CRUD, attempt flow, grading, CSV export, and deployment-ready configuration.
